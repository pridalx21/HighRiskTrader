[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "catalyst-mvp"
version = "0.1.0"
description = "Demo-only event-conditioned trading system MVP"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary - private research project" }
authors = [{ name = "CATALYST project" }]
dependencies = []

[project.optional-dependencies]
dev = [
  "mypy>=1.14,<2",
  "pytest>=8,<9",
  "pytest-cov>=5,<7",
  "ruff>=0.9,<1",
]
mt5 = ["MetaTrader5>=5.0"]
ui = ["plotly>=5.24,<7", "streamlit>=1.40,<2"]

[project.scripts]
catalyst-demo = "catalyst.demo:main"
catalyst-kill-switch = "catalyst.kill_switch_cli:main"
catalyst-mt5-shadow-smoke = "catalyst.mt5_shadow_smoke:main"
catalyst-run = "catalyst.run:main"
catalyst-validate = "catalyst.validation_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers --strict-config"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.coverage.run]
branch = true
source = ["catalyst"]
# Optional broker/UI adapters and command entry points have focused contract tests;
# the aggregate 85% gate measures deterministic executable and validation logic.
omit = [
  "src/catalyst/adapters/mt5_broker.py",
  "src/catalyst/adapters/mt5_exit.py",
  "src/catalyst/adapters/mt5_observability.py",
  "src/catalyst/dashboard.py",
  "src/catalyst/demo.py",
  "src/catalyst/kill_switch_cli.py",
  "src/catalyst/mt5_shadow_smoke.py",
  "src/catalyst/run.py",
  "src/catalyst/replay_demo.py",
  "src/catalyst/validation_cli.py",
]

[tool.coverage.report]
show_missing = true
fail_under = 85

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src"]
